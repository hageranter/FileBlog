
replicaCount: 1

image:
  repository: registry.arc-center.run/silverkeytech/fileblog/main
  pullPolicy: IfNotPresent
  tag: main-fileblog-dev-latest

imagePullSecrets: 
   - name: gitea-registry-secret

nameOverride: ""
fullnameOverride: ""

podAnnotations: {}
podLabels: {}

serviceAccount:
  create: false
  automount: true
  annotations: {}
  name: ""

podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "clusterissuer-arc-center-domain"
  hosts:
    - host: fileblog-main-dev.arc-center.run
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - fileblog-main-dev.arc-center.run
      secretName: fileblog-main-dev-tls

resources:
  requests:
    cpu: 50m
    memory: 50M

livenessProbe: null
readinessProbe: null

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

volumes: []
volumeMounts: []
nodeSelector: {}
tolerations: []
affinity: {}
envFrom:
  - secretRef:
      name: fileblog-main-dev
